nums = [4, 5, 6]
print(sum(nums, 10))  # Output: 25 (adds 10 to the sum)

#sum(iterable, start)

  '''  iterable: Required. The iterable whose items you want to sum
(e.g., list, tuple).

    start: Optional. The starting value (default is 0).'''

program: int is numeric: